import itertools

n = int(input())
a = []
for i in range(60):
    if n & (1 << i):
        a.append(i)

k = len(a)
res = []
t = list(itertools.combinations_with_replacement(range(2), k))
print(t)

# for i in (1 << k):
#     cur = 0